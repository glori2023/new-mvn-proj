
import errors.CarNotFoundException;
import model.Car;
import model.Person;

public class PersonsUtil {
    public static void drive(Person p){
   //     Car  car = p.getCar();
       // if ( car==null)
      //      throw new CarNotFoundException(); //leave the method
     //   System.out.println(p.getName()+"is driving"+p.getCar().getModel);
    }
}
